# todo: add unit test
# import unittest

# from panther_sdk import detection
# from panther_detections.providers import gsuite


# class TestRulesMobileDeviceScreenUnlockFail(unittest.TestCase):
#     def test_mobile_device_screen_unlock_fail(self) -> None:
#         name_override = "Override Name"
#         rule = gsuite.rules.mobile_device_screen_unlock_fail(
#             overrides=detection.RuleOverrides(name=name_override)
#         )

#         self.assertEqual(rule.name, name_override)
    